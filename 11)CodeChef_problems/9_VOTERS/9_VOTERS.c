#include<stdio.h>
int main(void)
{
    int N1, N2, N3;
    scanf("%d %d %d", &N1, &N2, &N3);
    int n1[N1], n2[N2], n3[N3];
    for(i=0; i<N1; i++)
        scanf("%d", &n1[i]);
    for(i=0; i<N2; i++)
        scanf("%d", &n2[i]);
    for(i=0; i<N3; i++)
        scanf("%d", &n3[i]);
    for(i=0; i<N1; i++)
    {
        for(i=0; i<N2; i++)
        {
            if(n1[i] == n2[i])
        }

    }

    return (0);
}
